https://kylestay.github.io/vibecodedpicker/

## Development

This picker is a static single-file app. Develop and verify it by opening `index.html` directly in the browser; do not start a local server unless a future change introduces a server-only dependency.

The CRT shader sources are embedded in `index.html` so the app works from direct `file://` access and from GitHub Pages.
